```
# Minimal Flask App

This is a simple Flask application with a single route (/) that returns "Hello from AI Agent". 
To run the app, install dependencies from `requirements.txt` and execute `app.py`.
```